# 🚀 คู่มือการ Deploy Mr.Promth ไปยัง Vercel

คู่มือฉบับสมบูรณ์สำหรับการ deploy โปรเจค Mr.Promth ไปยัง Vercel แบบละเอียด เหมาะสำหรับผู้ที่ไม่เคยใช้ Vercel มาก่อน

---

## 📋 สารบัญ

1. [Vercel คืออะไร?](#vercel-คืออะไร)
2. [เตรียมความพร้อมก่อน Deploy](#เตรียมความพร้อมก่อน-deploy)
3. [ขั้นตอนการ Deploy แบบละเอียด](#ขั้นตอนการ-deploy-แบบละเอียด)
4. [การตั้งค่า Environment Variables](#การตั้งค่า-environment-variables)
5. [การตรวจสอบหลัง Deploy](#การตรวจสอบหลัง-deploy)
6. [การแก้ไขปัญหาที่พบบ่อย](#การแก้ไขปัญหาที่พบบ่อย)
7. [การตั้งค่า Custom Domain](#การตั้งค่า-custom-domain)

---

## Vercel คืออะไร?

**Vercel** เป็นแพลตฟอร์มสำหรับ deploy และ host เว็บแอปพลิเคชัน โดยเฉพาะ Next.js (ซึ่งเป็นเฟรมเวิร์กที่ Vercel พัฒนาขึ้นเอง) มีข้อดีดังนี้:

✅ **ฟรีสำหรับ Hobby Projects** - ไม่ต้องใส่บัตรเครดิต  
✅ **Deploy ง่ายมาก** - เชื่อมต่อ GitHub แล้วคลิกเดียว  
✅ **SSL Certificate อัตโนมัติ** - HTTPS ฟรีทันที  
✅ **Global CDN** - เว็บเร็วทั่วโลก  
✅ **Auto Scaling** - รองรับผู้ใช้เยอะขึ้นอัตโนมัติ  
✅ **Preview Deployments** - ทุก Pull Request จะได้ URL ทดสอบ  

---

## เตรียมความพร้อมก่อน Deploy

### 1. ✅ Supabase Project

คุณต้องมี Supabase project ที่พร้อมใช้งาน:

- **URL**: `https://xcwkwdoxrbzzpwmlqswr.supabase.co`
- **ต้องทำ**:
  - ✅ รัน database migrations (ไฟล์ใน `database/migrations/`)
  - ✅ เปิดใช้งาน Authentication
  - ✅ ตั้งค่า Row Level Security (RLS) policies
  - ✅ เก็บ credentials ไว้:
    - `NEXT_PUBLIC_SUPABASE_URL`
    - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
    - `SUPABASE_SERVICE_ROLE_KEY`

**วิธีหา Supabase Credentials:**

1. ไปที่ [Supabase Dashboard](https://supabase.com/dashboard)
2. เลือก project ของคุณ
3. ไปที่ **Settings** → **API**
4. คัดลอก:
   - **Project URL** → `NEXT_PUBLIC_SUPABASE_URL`
   - **anon public** → `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - **service_role** → `SUPABASE_SERVICE_ROLE_KEY` (⚠️ เก็บเป็นความลับ!)

### 2. ✅ VanchinAI API Key

คุณต้องมี VanchinAI API key:

1. ไปที่ [VanchinAI](https://api.vanchin.ai)
2. สมัครสมาชิก/Login
3. ไปที่ **API Keys** section
4. สร้าง API key ใหม่
5. คัดลอก key → `VANCHIN_API_KEY`

### 3. ✅ GitHub Repository

- **Repository**: `donlasahachat11-lgtm/mrphomth`
- **Branch**: `phase-1-foundation`
- ✅ ตรวจสอบว่า code ล่าสุด push แล้ว

---

## ขั้นตอนการ Deploy แบบละเอียด

### ขั้นตอนที่ 1: สร้างบัญชี Vercel

1. ไปที่ [https://vercel.com](https://vercel.com)
2. คลิก **"Sign Up"** มุมขวาบน
3. เลือก **"Continue with GitHub"**
4. Login ด้วย GitHub account ของคุณ
5. Authorize Vercel ให้เข้าถึง GitHub ของคุณ

### ขั้นตอนที่ 2: Import Repository

1. หลังจาก Login แล้ว คุณจะเห็นหน้า Dashboard
2. คลิกปุ่ม **"Add New..."** → เลือก **"Project"**
3. คุณจะเห็นรายการ repositories จาก GitHub
4. ค้นหา **"mrphomth"** หรือ scroll หา repository
5. คลิก **"Import"** ที่ repository `donlasahachat11-lgtm/mrphomth`

### ขั้นตอนที่ 3: Configure Project

หน้าต่าง Configure Project จะแสดงขึ้นมา:

#### 3.1 Project Name
- **ชื่อ**: `mrphomth` (หรือชื่อที่คุณต้องการ)
- ชื่อนี้จะเป็นส่วนหนึ่งของ URL: `https://mrphomth.vercel.app`

#### 3.2 Framework Preset
- Vercel จะ **auto-detect** ว่าเป็น **Next.js** ✅
- ถ้าไม่ auto-detect ให้เลือก **"Next.js"** เอง

#### 3.3 Root Directory
- ใช้ค่า default: **"./"** (root ของ repository)

#### 3.4 Build and Output Settings
- **Build Command**: `npm run build` (default ✅)
- **Output Directory**: `.next` (default ✅)
- **Install Command**: `npm install` (default ✅)

#### 3.5 Environment Variables (สำคัญมาก!)

คลิก **"Environment Variables"** เพื่อเพิ่มตัวแปรต่อไปนี้:

| Variable Name | Value | คำอธิบาย |
|---------------|-------|----------|
| `NEXT_PUBLIC_SUPABASE_URL` | `https://xcwkwdoxrbzzpwmlqswr.supabase.co` | URL ของ Supabase project |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | `eyJhbGc...` | Public anon key จาก Supabase |
| `SUPABASE_SERVICE_ROLE_KEY` | `eyJhbGc...` | Service role key (⚠️ เก็บเป็นความลับ) |
| `VANCHIN_API_KEY` | `vch_...` | API key จาก VanchinAI |

**วิธีเพิ่ม Environment Variables:**

1. คลิก **"Add"** หรือ **"Environment Variables"**
2. ใส่ **Key** (ชื่อตัวแปร)
3. ใส่ **Value** (ค่าของตัวแปร)
4. เลือก **Environment**: `Production`, `Preview`, และ `Development` (เลือกทั้ง 3 ✅)
5. คลิก **"Add"**
6. ทำซ้ำสำหรับทุกตัวแปร

### ขั้นตอนที่ 4: Deploy!

1. ตรวจสอบการตั้งค่าทั้งหมดอีกครั้ง
2. คลิกปุ่ม **"Deploy"** สีน้ำเงินใหญ่ๆ
3. Vercel จะเริ่ม build และ deploy โปรเจคของคุณ

### ขั้นตอนที่ 5: รอ Build เสร็จ

- Vercel จะแสดงหน้า Building...
- คุณจะเห็น logs แบบ real-time
- ใช้เวลาประมาณ **2-4 นาที**
- เมื่อเสร็จจะเห็นข้อความ **"Congratulations!"** 🎉

### ขั้นตอนที่ 6: เข้าถึงเว็บไซต์ของคุณ

- คลิกปุ่ม **"Visit"** หรือ **"Go to Dashboard"**
- URL ของคุณจะเป็น: `https://mrphomth.vercel.app` (หรือชื่อที่คุณตั้ง)
- คัดลอก URL และทดสอบเว็บไซต์!

---

## การตั้งค่า Environment Variables

### วิธีเพิ่ม/แก้ไข Environment Variables หลัง Deploy

1. ไปที่ [Vercel Dashboard](https://vercel.com/dashboard)
2. เลือก project **"mrphomth"**
3. ไปที่ tab **"Settings"**
4. เลือก **"Environment Variables"** จากเมนูด้านซ้าย
5. คลิก **"Add New"** เพื่อเพิ่มตัวแปรใหม่
6. หรือคลิก **"Edit"** เพื่อแก้ไขตัวแปรเดิม
7. **สำคัญ**: หลังแก้ไข environment variables ต้อง **Redeploy** ใหม่!

### วิธี Redeploy

1. ไปที่ tab **"Deployments"**
2. เลือก deployment ล่าสุด
3. คลิกปุ่ม **"..."** (three dots)
4. เลือก **"Redeploy"**
5. รอ build เสร็จ

---

## การตรวจสอบหลัง Deploy

### 1. ทดสอบ Health Check

เปิด terminal และรันคำสั่ง:

```bash
curl https://your-app.vercel.app/api/health
```

**Expected Response:**
```json
{
  "status": "healthy",
  "timestamp": "2024-11-07T...",
  "services": {
    "database": { "status": "healthy" },
    "authentication": { "status": "healthy" }
  }
}
```

### 2. ทดสอบหน้าแรก

เปิดเบราว์เซอร์และไปที่: `https://your-app.vercel.app`

ตรวจสอบ:
- ✅ โลโก้แสดงถูกต้อง
- ✅ Theme toggle ทำงาน (Dark/Light)
- ✅ ปุ่มต่างๆ responsive และมี animations
- ✅ ไม่มี console errors

### 3. ทดสอบ Authentication

1. คลิก **"Get Started"** หรือ **"Sign Up"**
2. สร้างบัญชีใหม่
3. ตรวจสอบว่าได้รับ email verification
4. Login เข้าสู่ระบบ
5. ทดสอบ logout

### 4. ทดสอบ Dashboard

1. Login เข้าสู่ระบบ
2. ไปที่ `/app/dashboard`
3. ทดสอบสร้าง project ใหม่
4. ตรวจสอบว่า agent chain ทำงาน

### 5. ตรวจสอบ Logs

1. ไปที่ Vercel Dashboard
2. เลือก project ของคุณ
3. ไปที่ tab **"Logs"**
4. ดู real-time logs เพื่อตรวจสอบ errors

---

## การแก้ไขปัญหาที่พบบ่อย

### ❌ Build Failed

**สาเหตุที่พบบ่อย:**

1. **Missing Environment Variables**
   - ตรวจสอบว่าใส่ environment variables ครบทั้ง 4 ตัว
   - ตรวจสอบว่าไม่มี typo ในชื่อตัวแปร

2. **TypeScript Errors**
   - ดู build logs ใน Vercel
   - แก้ไข errors ใน code
   - Push code ใหม่ไปยัง GitHub

3. **Dependency Issues**
   - ตรวจสอบ `package.json`
   - ลองรัน `npm install` locally
   - ตรวจสอบว่า `package-lock.json` ถูก commit

**วิธีแก้:**
1. ดู detailed logs ใน Vercel
2. แก้ไขปัญหาใน code
3. Push ไปยัง GitHub
4. Vercel จะ auto-deploy ใหม่

### ❌ Database Connection Failed

**Error**: `supabaseUrl is required` หรือ `Invalid API key`

**วิธีแก้:**
1. ตรวจสอบ environment variables ใน Vercel Settings
2. ตรวจสอบว่า Supabase credentials ถูกต้อง
3. ตรวจสอบว่า Supabase project ยังทำงานอยู่
4. Redeploy หลังแก้ไข

### ❌ 404 Not Found

**สาเหตุ:**
- Route ไม่ถูกต้อง
- Page ไม่ได้ถูก build

**วิธีแก้:**
1. ตรวจสอบ URL ว่าถูกต้อง
2. ดู build logs ว่า page ถูก generate
3. ตรวจสอบ file structure ใน repository

### ❌ Theme Toggle ไม่ทำงาน

**สาเหตุ:**
- ThemeProvider ไม่ได้ wrap app
- localStorage ถูก block

**วิธีแก้:**
1. ตรวจสอบ `app/layout.tsx` ว่ามี `<ThemeProvider>`
2. ตรวจสอบ browser console สำหรับ errors
3. ลอง hard refresh (Ctrl+Shift+R)

### ❌ Images ไม่โหลด

**สาเหตุ:**
- Image path ไม่ถูกต้อง
- Next.js Image Optimization issues

**วิธีแก้:**
1. ตรวจสอบว่าไฟล์อยู่ใน `public/` folder
2. ใช้ path เป็น `/logo-with-text.webp` (เริ่มด้วย `/`)
3. ตรวจสอบ `next.config.mjs` สำหรับ image settings

---

## การตั้งค่า Custom Domain

### ขั้นตอนที่ 1: เพิ่ม Domain ใน Vercel

1. ไปที่ Vercel Dashboard
2. เลือก project ของคุณ
3. ไปที่ tab **"Settings"**
4. เลือก **"Domains"**
5. คลิก **"Add"**
6. ใส่ domain ของคุณ เช่น `mrpromth.com`
7. คลิก **"Add"**

### ขั้นตอนที่ 2: ตั้งค่า DNS

Vercel จะแสดงค่า DNS records ที่ต้องเพิ่ม:

**Option A: A Record (แนะนำ)**
```
Type: A
Name: @
Value: 76.76.21.21
```

**Option B: CNAME Record**
```
Type: CNAME
Name: www
Value: cname.vercel-dns.com
```

### ขั้นตอนที่ 3: เพิ่ม DNS Records ที่ Domain Provider

1. ไปที่ domain provider ของคุณ (GoDaddy, Namecheap, etc.)
2. ไปที่ DNS Management
3. เพิ่ม A Record หรือ CNAME Record ตามที่ Vercel แจ้ง
4. Save changes

### ขั้นตอนที่ 4: รอ DNS Propagation

- DNS propagation ใช้เวลา **5-10 นาที** ถึง **48 ชั่วโมง**
- ตรวจสอบสถานะใน Vercel Dashboard
- เมื่อเสร็จจะเห็น **"Valid Configuration"** ✅

### ขั้นตอนที่ 5: SSL Certificate

- Vercel จะสร้าง SSL certificate อัตโนมัติ
- HTTPS จะทำงานทันที
- ไม่ต้องตั้งค่าอะไรเพิ่มเติม! 🎉

---

## 🎉 สรุป

คุณได้ deploy Mr.Promth ไปยัง Vercel เรียบร้อยแล้ว!

### ✅ สิ่งที่คุณได้

- ✅ เว็บไซต์ที่ทำงานบน production
- ✅ HTTPS/SSL certificate ฟรี
- ✅ Global CDN สำหรับความเร็ว
- ✅ Auto scaling สำหรับ traffic
- ✅ Preview deployments สำหรับ PRs
- ✅ Real-time logs และ monitoring

### 📚 Resources เพิ่มเติม

- [Vercel Documentation](https://vercel.com/docs)
- [Next.js Documentation](https://nextjs.org/docs)
- [Supabase Documentation](https://supabase.com/docs)
- [VanchinAI Documentation](https://api.vanchin.ai/docs)

### 🆘 ต้องการความช่วยเหลือ?

- [Vercel Support](https://vercel.com/support)
- [Vercel Community](https://github.com/vercel/vercel/discussions)
- [Next.js Discord](https://nextjs.org/discord)

---

**Happy Deploying! 🚀**
